@* Community Explorer is now a tab within the main Search page *@
@page "/communities"
@inject NavigationManager Nav

@code {
    /// <inheritdoc />
    protected override void OnInitialized()
    {
        Nav.NavigateTo("/", replace: true);
    }
}
